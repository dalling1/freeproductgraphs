Free Products of Graphs
---

Draws the free product of user-specified graphs.

The free product of a set of graphs is formed by connecting a copy of
every other graph to each node of each graph.  The specific nodes which
are connected at each step are defined by the "update function" of each
node.

A live version is at https://dalling1.github.io/freeproductgraphs/
